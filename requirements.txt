requests>=2.31
python-dotenv>=1.0
# Optional canonical AI platform (public contracts only):
#   pip install "yasinai @ git+https://github.com/yusi20006-max/Yasin-AI.git@v1.1.4"
# or: pip install -e ../Yasin-AI
# Without yasinai, AI_PROVIDER=yasinai raises RuntimeError (no silent fallback).
